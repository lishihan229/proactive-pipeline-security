repos:
  - repo: local
    hooks:
      - id: pipeline-security
        name: proactive pipeline security
        entry: bash -c 'PYTHONPATH=src python3 -m pipeline_security --staged'
        language: system
        pass_filenames: false
        stages: [pre-commit]
