repos:
  - repo: local
    hooks:
      - id: pipeline-security
        name: proactive pipeline security
        entry: python3 -m pipeline_security --staged
        language: system
        pass_filenames: false
        stages: [pre-commit]
